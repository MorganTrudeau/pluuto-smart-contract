{
  "name": "pluuto-smart-contract",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "rm -rf cache/ artifacts/ dist/",
    "deploy:sepolia": "hardhat run --network sepolia scripts/deploy.ts",
    "deploy:base": "hardhat run --network base scripts/deploy.ts",
    "interact:sepolia": "hardhat run --network sepolia scripts/interact.ts",
    "interact:base": "hardhat run --network base scripts/interact.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.16.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "chai": "^4.3.7",
    "ethers": "^6.9.0",
    "mocha": "^10.2.0"
  }
}
